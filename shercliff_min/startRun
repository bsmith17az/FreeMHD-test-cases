#!/bin/bash
. ${WM_PROJECT_DIR:?}/bin/tools/RunFunctions        #  run functions
#------------------------------------------------------------------------------

#run the simulation
srun -n 4 epotMultiRegionInterFoam -parallel 2>&1 | tee ./runLog

#------------------------------------------------------------------------------
